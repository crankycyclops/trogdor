-- takes as input the object's name (used to identify object to the API)
function beforeTakeObject(name)

   -- this is just a placeholder for now
   io.write("The candle jumps up in the air, does a backflip and transforms into a spoon. ");
   return ALLOW_ACTION

end
